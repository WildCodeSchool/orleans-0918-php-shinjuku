{% extends 'layout.html.twig' %}
{%block title %}{{ parent() }} - Article{% endblock %}
{% block banner %} <div class="banner2"></div>{% endblock %}
{% block content %}
<section class="product">
    <!-- Product -->
    <div class="bg0 m-t-23 p-b-140">
        <div class="container">
            <!--Search bar-->
            <div class="row d-flex justify-content-center bd-highlight mb-3  search_product_bar">

                <nav class="m-b-50 search_bar col-md-12 col-lg-5 col-xs-12 col-sm-12  navbar navbar-light">
                    <a href="/article/add" class="add btn btn-success btn-sm" name="add" value="add"><i class="fas fa-plus"></i> Ajouter un nouvel article</a>

                </nav>
            </div>
        </div>
    </div>
    <!-- Block2 -->
    <div class="container">
        <div class="row ">
            <div class="col-lg-12 bold cl13 fs-30 d-flex justify-content-center">{{ error.category }} {{ error.toomuch }}</div>
            {% for article in articles %}
                <div class="col-xs-12 col-sm-6 col-md-6 col-lg-3 m-b-35 block-article">
                    <div class="block2">
                        <div class="block2-pic hov-img0">

                            <img src="/assets/images/upload/{{ article.picture }}" alt="image d'{{ article.name }}" class="image-article" >
                        </div>
                        <div class="block2-txt flex-w flex-t p-t-14">
                            <div class="block2-txt-child1 flex-col-l ">
                                <a href="/article/{{ article.id }}" class=" cl10 hov-cl1 trans-04  p-b-6 bold title overflow">
                                    {{ article.name }}
                                </a>
                                <div class="d-flex flex-row article-price">
                                    <div>
                                        <span class="badge badge-primary badge-price bold badge-admin">Prix:{{ article.price }}€</span>
                                    </div>
                                    <div class="ml-auto">
                                        <span class="badge badge-primary badge-category-admin">{{ article.category }}</span>
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>


                    <a href="/admin/edit/{{ article.id }}"  class="update btn btn-primary btn-sm btn-admin btn-admin1" name="update" value="update"><i class="fas fa-pen"></i> Modifier</a>
                    <a href="/admin/list/{{ article.id }}"  onclick="return confirm('Etes vous sur de vouloir supprimer cet article')" class="delete btn btn-danger btn-sm btn-admin" NAME="supprimer" VALUE="supprimer"><i class="fas fa-trash-alt"></i>  Supprimer</a>



                </div>
            {% endfor %}
        </div>
    </div>
    {% endblock %}
