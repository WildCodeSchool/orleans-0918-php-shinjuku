{% extends 'layout.html.twig' %}

{% block content %}

    <!-- Title page -->
    <section class=" bg-img1 txt-center p-lr-15 p-tb-92">
        <h2 class=" m-t-70 ltext-105 cl0 txt-center">
            Accueil
        </h2>
    </section>

    <!-- Content page -->
    <section class="bg0 p-t-75 p-b-120">
        <div class="container">
            <div class="row">
                <div class="order-md-2 col-md-7 col-lg-8 p-b-30">
                    <div class="p-t-7 p-l-85 p-l-15-lg p-l-0-md">
                        <h3 class="mtext-111 cl2 p-b-16">Un petit résumé de qui sommes nous ?</h3>
                        <p class="stext-113  p-b-26">
                        Cette boutique de référence de la culture japonaise est remplie du sol au plafond de mangas, de revues portant sur les mangas et le japon, de DVD d'animés, de maquettes, et de goodies.... de mangas !
                        </p>
                    </div>
                </div>

                <div class="order-md-1 col-11 col-md-5 col-lg-4 m-lr-auto p-b-30">
                    <div class="">
                        <div class="">
                            <img src="../../../assets/images/logo.png" alt="logo">
                        </div>
                    </div>
                </div>
            </div>

            {% include 'Home/facebook_api.html.twig' %}

            <div class="row">
                <h2 class="col-lg-12">Nos Nouveautés</h2>
                {% for key, value in article %}

                    <div class="col-sm-4 mx-auto">
                        <div class="card">
                            <img class="card-img-top mx-auto" src="{ {{ value.picture}}" alt="Card image cap">
                            <div class="card-body">
                                <h5 class="card-title"> {{ value.name}}</h5>
                                {#<p class="card-text">#}
                                {#{{ value.description}}#}
                                {#</p>#}
                                <div class="card-text" class="text bold"> {{ value.price}} €</div>
                            </div>
                        </div>
                    </div>
                {% endfor %}

            </div>
        </div>
    </div>
</section>

{% endblock %}

